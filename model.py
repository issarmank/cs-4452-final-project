import torch
import torch.nn as nn
import torch.optim as optim

class DynamicNN(nn.Module):
    def __init__(self, input_size, hidden_sizes, num_layers, output_size):
        super().__init__()
        #Make the rest of this later
    
    def forward(self, x):
        # Will work on this later
        pass